## Diagnostic Results

**Reproduced:** {yes | no | partially}

**Environment:** {staging | production | both | {specific}}

**Root Cause:**

{The clear explanation of the root cause. Format: "The bug occurs because [cause], which causes [effect]."}

**Affected Files:**
- `path/to/file` — {what's wrong}

**Minimum Reproduction:**
{Steps to reproduce with minimal setup}

**Hypotheses Tested:**
1. {Hypothesis 1} — {confirmed | rejected}
2. {Hypothesis 2} — {confirmed | rejected}
3. {Hypothesis 3} — {confirmed | rejected}

{If regression detected:}
**Regression:** Yes — introduced in commit `{hash}`

{If research findings:}
**Similar Issues:**
- [{title}]({url}) — {brief note}

---
*Diagnostic by Claude Code*
